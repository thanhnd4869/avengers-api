NODE_ENV=development
HOST=localhost
PORT=8000
LOG_LEVEL=debug

# Comma-separated list; empty allows all origins during development
CORS_ORIGINS=

# MongoDB Atlas connection string
MONGODB_URI=mongodb+srv://user:password@cluster.mongodb.net/avengers?retryWrites=true&w=majority
